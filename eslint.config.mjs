// Covers repo-root TS config files only. Each package has its own
// eslint.config.mjs, which is authoritative for that package's files.
import { rootConfig } from '@my-project/configs/eslint';

export default rootConfig;
